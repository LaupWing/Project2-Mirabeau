<section class="room centering">
    <h2 class="<%=room.occupancy%>"><%= room.roomName %></h2>
    <div class="currentInfo centering">
        <p>Temperature</p>
        <h3>
            <%= room.temperature %> c&deg
        </h3>
    </div>
    <nav>
        <% keys.forEach(key=>{ %>
            <%= key %>
        <% }) %>
    </nav>
</section>